# 실제 값은 .env 에 넣고, .env 는 절대 커밋하지 마세요.
PORT=3000

DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=app_readonly
DB_PASSWORD=
DB_NAME=asset_mgmt
DB_CONNECTION_LIMIT=10

# 클라이언트가 x-api-key 헤더로 보내야 하는 값
# 생성 예: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
API_KEY=
